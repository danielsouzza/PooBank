<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.danielsouzza.poobank">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.POOBank">
        <activity android:name=".ExtractActivity"></activity>
        <activity android:name=".RegisterKeyPixActivity" />
        <activity android:name=".ManageKeysPixActivity" />
        <activity android:name=".FinishedTransactionActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".TransferEndActivity" />
        <activity android:name=".DepositActivity" />
        <activity android:name=".TransferStartActivity" />
        <activity android:name=".RegisterAccountActivity" />
        <activity android:name=".RegisterClientActivity" />
        <activity
            android:name=".HomeActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".FinishedTransactionActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>